{
  "name": "work-boost",
  "version": "0.1.0",
  "description": "Work Boost - A productivity tool for managing daily work tasks",
  "type": "module",
  "scripts": {
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "biome format --write .",
    "format:check": "biome format --check .",
    "check": "npm run lint && npm run format:check",
    "fix": "npm run lint:fix && npm run format"
  },
  "dependencies": {
    "@biomejs/biome": "^1.9.4",
    "oxlint": "^0.11.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
